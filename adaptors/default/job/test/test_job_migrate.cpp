
#include <stdlib.h>
#include <signal.h>

#include <saga/saga.hpp>


int main (int argc, char** argv)
{
  int err = 0;

  // migrate is implemented, but not really usable at all.  So, we rather don't
  // attempt to test it right now...

  return err; 
}

